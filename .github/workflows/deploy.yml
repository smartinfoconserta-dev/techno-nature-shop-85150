name: Deploy Lovable via FTP/SFTP na Hostinger

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Clonar o repositório
      - name: 📥 Clonar o repositório
        uses: actions/checkout@v3

      # 2️⃣ Configurar Node.js
      - name: ⚙️ Configurar Node.js 18
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # 3️⃣ Instalar dependências
      - name: 📦 Instalar dependências
        run: npm install

      # 4️⃣ Gerar build de produção
      - name: 🏗️ Gerar build de produção
        run: npm run build

      # 5️⃣ Mostrar arquivos gerados
      - name: 📁 Mostrar arquivos gerados
        run: ls -al dist/

      # 6️⃣ Deploy via FTPS (padrão)
      - name: 🚀 Deploy via FTPS para Hostinger
        id: ftp-deploy
        continue-on-error: true
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          port: 21
          protocol: ftps
          security: loose
          local-dir: dist/
          server-dir: ${{ secrets.FTP_PATH }}
          dangerous-clean-slate: true
          dry-run: false
          log-level: verbose

      # 7️⃣ Se FTPS falhar, tenta SFTP automaticamente
      - name: 🔁 Fallback para SFTP (caso FTPS falhe)
        if: steps.ftp-deploy.outcome == 'failure'
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          port: 22
          local_path: dist/*
          remote_path: ${{ secrets.FTP_PATH }}
          sftp_only: true
          delete_remote_files: true
