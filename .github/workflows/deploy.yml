name: Deploy Lovable via FTP na Hostinger

on:
  push:
    branches:
      - main   # Executa o deploy automático sempre que fizer push na branch main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Clona o repositório
      - name: 📥 Clonar o repositório
        uses: actions/checkout@v3

      # 2️⃣ Configura a versão do Node.js
      - name: ⚙️ Configurar Node.js 18
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # 3️⃣ Instala as dependências
      - name: 📦 Instalar dependências
        run: npm install

      # 4️⃣ Gera o build de produção (Lovable → Vite → dist/)
      - name: 🏗️ Gerar build de produção
        run: npm run build

      # 5️⃣ Mostra os arquivos gerados (para debug)
      - name: 📁 Mostrar arquivos gerados
        run: ls -al dist/

      # 6️⃣ Realiza o deploy automático para a Hostinger (via FTPS seguro)
      - name: 🚀 Deploy FTP para Hostinger (FTPS seguro)
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASS }}
          port: 21
          protocol: ftps
          security: explicit
          local-dir: dist/
          server-dir: /public_html/
          dangerous-clean-slate: true
          dry-run: false
          log-level: verbose
